# See uscan(1) for format

# Compulsory line, this is a version 3 file
version=3

# As we still didn't settle for the proper place to put source tarballs,
# this file doesn't contain any link to package

# Uncommment to examine a FTP server
#ftp://ftp.example.com/pub/#PACKAGE#-(.*)\.tar\.gz debian uupdate

# Uncomment to find new files on sourceforge, for devscripts >= 2.9
http://sf.net/linux-zigbee/lowpan-tools-(.*)\.tar\.gz

# Uncomment to find new files on GooglePages
# http://example.googlepages.com/foo.html #PACKAGE#-(.*)\.tar\.gz
